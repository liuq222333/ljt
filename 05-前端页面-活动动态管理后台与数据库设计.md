# 社区服务智能触达系统 - 详细技术文档（续4）

## 前端页面详细说明（续）

#### 10.5 本地活动模块

##### 10.5.1 LocalAct.vue
**路径**: `/local-act`
**功能**: 本地活动首页
**主要功能**:
- 活动列表展示
- 分类筛选
- 时间筛选
- 附近活动推荐
- 活动搜索

**API调用**:
- `GET /api/local-act/activities/list` - 活动列表
- `GET /api/local-act/activities/nearby` - 附近活动

##### 10.5.2 PublishActivity.vue
**路径**: `/local-act/publish`
**功能**: 发布活动
**主要功能**:
- 活动信息填写
- 活动时间选择
- 地点选择（地图）
- 人数限制
- 联系方式
- 标签设置
- 提交审核

**API调用**:
- `POST /api/local-act/activities` - 创建活动

##### 10.5.3 ActivityDetail.vue
**路径**: `/local-act/activity/:id`
**功能**: 活动详情页
**主要功能**:
- 活动详细信息
- 报名按钮
- 参与人数
- 活动地图
- 评论互动
- 分享功能

**API调用**:
- `GET /api/local-act/activities/{id}` - 活动详情
- `POST /api/local-act/enroll` - 报名活动

##### 10.5.4 MyEnrollments.vue
**路径**: `/local-act/my-enrollments`
**功能**: 我的报名
**主要功能**:
- 报名记录列表
- 状态筛选（待参加/已参加/已取消）
- 时间筛选
- 取消报名
- 签到功能

**API调用**:
- `GET /api/local-act/enrollments` - 获取报名记录

##### 10.5.5 MapView.vue
**路径**: `/local-act/map`
**功能**: 地图视图
**主要功能**:
- 地图展示活动位置
- 标记点击查看详情
- 当前位置定位
- 附近活动搜索

##### 10.5.6 NotificationsCenter.vue
**路径**: `/local-act/notifications`
**功能**: 活动通知中心
**主要功能**:
- 活动提醒
- 报名通知
- 活动变更通知

##### 10.5.7 ScheduleTemplates.vue
**路径**: `/local-act/templates`
**功能**: 日程模板
**主要功能**:
- 查看固定日程
- 创建日程模板
- 订阅日程

**API调用**:
- `POST /api/local-act/schedule-templates` - 创建模板
- `GET /api/local-act/schedule-templates` - 获取模板列表

##### 10.5.8 NeighborSupport.vue
**路径**: `/local-act/neighbor-support`
**功能**: 邻里互助
**主要功能**:
- 互助任务列表
- 发布互助任务
- 接受任务
- 任务状态管理

**API调用**:
- `GET /api/neighbor-support/tasks` - 任务列表
- `POST /api/neighbor-support/tasks` - 发布任务

##### 10.5.9 StoriesArchive.vue
**路径**: `/local-act/stories`
**功能**: 故事档案列表
**主要功能**:
- 社区故事列表
- 故事分类
- 故事搜索

##### 10.5.10 StoriesArchiveDetail.vue
**路径**: `/local-act/stories/:id`
**功能**: 故事详情
**主要功能**:
- 故事内容展示
- 图片展示
- 评论互动

##### 10.5.11 StoriesArchivePublish.vue
**路径**: `/local-act/stories/publish`
**功能**: 发布故事
**主要功能**:
- 故事标题
- 故事内容编辑
- 图片上传
- 标签设置

---

#### 10.6 社区动态模块

##### 10.6.1 CommunityFeed.vue
**路径**: `/feed`
**功能**: 社区动态首页
**主要功能**:
- 动态信息流
- 下拉刷新
- 上拉加载更多
- 点赞/评论
- 发布动态入口

**API调用**:
- `GET /api/community-feed/posts` - 动态列表
- `POST /api/community-feed/posts/{id}/like` - 点赞
- `DELETE /api/community-feed/posts/{id}/like` - 取消点赞

##### 10.6.2 publishPost.vue
**路径**: `/feed/publish`
**功能**: 发布动态
**主要功能**:
- 文字内容输入
- 图片上传（多图）
- 位置选择
- 可见性设置（公开/好友/私密）
- 发布

**API调用**:
- `POST /api/community-feed/upload` - 上传图片
- `POST /api/community-feed/posts` - 发布动态

---

#### 10.7 社区服务模块

##### 10.7.1 CommunityService.vue
**路径**: `/service`
**功能**: 社区服务页面
**主要功能**:
- 服务分类展示
- 服务列表
- 服务详情
- 服务预约

---

#### 10.8 通知模块

##### 10.8.1 Notification.vue
**路径**: `/notification`
**功能**: 通知中心
**主要功能**:
- 通知列表
- 未读标记
- 标记已读
- 删除通知
- 通知分类

**WebSocket连接**:
- 实时接收新通知
- 未读数量实时更新

---

### 管理后台前端 (front-admin)

#### 11.1 管理后台页面

##### 11.1.1 Home.vue
**路径**: `/admin`
**功能**: 管理后台首页
**主要功能**:
- 数据统计概览
- 用户数量
- 活动数量
- 商品数量
- 待审核数量

##### 11.1.2 UserManagement.vue
**路径**: `/admin/users`
**功能**: 用户管理
**主要功能**:
- 用户列表
- 用户搜索
- 用户详情
- 用户状态管理
- 用户权限管理

**API调用**:
- `GET /api/admin/users` - 用户列表
- `POST /api/admin/users/{id}/status` - 修改状态

##### 11.1.3 ActivityReview.vue
**路径**: `/admin/activities/review`
**功能**: 活动审核
**主要功能**:
- 待审核活动列表
- 活动详情查看
- 审核通过
- 审核拒绝
- 审核备注

**API调用**:
- `GET /api/admin/local-act/reviews` - 待审核列表
- `POST /api/admin/local-act/reviews/{id}/approve` - 通过
- `POST /api/admin/local-act/reviews/{id}/reject` - 拒绝

##### 11.1.4 TaskReview.vue
**路径**: `/admin/tasks/review`
**功能**: 任务审核
**主要功能**:
- 待审核任务列表
- 任务详情
- 审核操作

##### 11.1.5 NotificationPublish.vue
**路径**: `/admin/notifications/publish`
**功能**: 通知发布
**主要功能**:
- 通知标题
- 通知内容
- 目标用户选择
- 优先级设置
- 定时发送

**API调用**:
- `POST /api/admin/notifications/add` - 发布通知

##### 11.1.6 MarketProductsManagement.vue
**路径**: `/admin/products`
**功能**: 商品管理
**主要功能**:
- 商品列表
- 商品搜索
- 商品审核
- 商品下架
- 违规处理

##### 11.1.7 ApiManagement.vue
**路径**: `/admin/api`
**功能**: API管理
**主要功能**:
- API路由配置
- 工具配置
- API测试

---

## 数据库设计

### 数据表详细说明

#### 12.1 用户相关表

##### 12.1.1 user（用户表）
```sql
CREATE TABLE `user` (
  `user_id` VARCHAR(50) PRIMARY KEY COMMENT '用户ID',
  `user_name` VARCHAR(100) NOT NULL UNIQUE COMMENT '用户名',
  `password` VARCHAR(255) NOT NULL COMMENT '密码（加密）',
  `phone` VARCHAR(20) COMMENT '手机号',
  `email` VARCHAR(100) COMMENT '邮箱',
  `avatar_key` VARCHAR(255) COMMENT '头像对象键',
  `address` VARCHAR(255) COMMENT '地址',
  `bio` TEXT COMMENT '个人简介',
  `status` TINYINT DEFAULT 1 COMMENT '状态：0-禁用，1-正常',
  `created_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  INDEX idx_user_name (`user_name`),
  INDEX idx_phone (`phone`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';
```

---

#### 12.2 社区集市相关表

##### 12.2.1 product（商品表）
```sql
CREATE TABLE `product` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '商品ID',
  `name` VARCHAR(200) NOT NULL COMMENT '商品名称',
  `description` TEXT COMMENT '商品描述',
  `price` DECIMAL(10,2) NOT NULL COMMENT '价格',
  `stock` INT DEFAULT 0 COMMENT '库存',
  `category_id` INT COMMENT '分类ID',
  `seller_id` INT NOT NULL COMMENT '卖家ID',
  `location` VARCHAR(255) COMMENT '位置',
  `latitude` DECIMAL(10,7) COMMENT '纬度',
  `longitude` DECIMAL(10,7) COMMENT '经度',
  `image_urls` TEXT COMMENT '图片URL（JSON数组）',
  `status` TINYINT DEFAULT 1 COMMENT '状态：0-下架，1-上架',
  `created_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  INDEX idx_category (`category_id`),
  INDEX idx_seller (`seller_id`),
  INDEX idx_status (`status`),
  INDEX idx_price (`price`),
  SPATIAL INDEX idx_location (`latitude`, `longitude`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='商品表';
```

##### 12.2.2 category（分类表）
```sql
CREATE TABLE `category` (
  `id` INT PRIMARY KEY AUTO_INCREMENT COMMENT '分类ID',
  `name` VARCHAR(100) NOT NULL COMMENT '分类名称',
  `parent_id` INT DEFAULT 0 COMMENT '父分类ID',
  `level` TINYINT DEFAULT 1 COMMENT '层级',
  `sort_order` INT DEFAULT 0 COMMENT '排序',
  `icon` VARCHAR(255) COMMENT '图标',
  `created_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  INDEX idx_parent (`parent_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='商品分类表';
```

##### 12.2.3 cart（购物车表）
```sql
CREATE TABLE `cart` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '购物车ID',
  `user_name` VARCHAR(100) NOT NULL COMMENT '用户名',
  `product_id` BIGINT NOT NULL COMMENT '商品ID',
  `quantity` INT DEFAULT 1 COMMENT '数量',
  `created_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '添加时间',
  `updated_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  UNIQUE KEY uk_user_product (`user_name`, `product_id`),
  INDEX idx_user (`user_name`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='购物车表';
```

##### 12.2.4 orders（订单表）
```sql
CREATE TABLE `orders` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '订单ID',
  `order_no` VARCHAR(50) UNIQUE NOT NULL COMMENT '订单号',
  `user_name` VARCHAR(100) NOT NULL COMMENT '买家用户名',
  `product_id` BIGINT NOT NULL COMMENT '商品ID',
  `product_name` VARCHAR(200) COMMENT '商品名称',
  `quantity` INT NOT NULL COMMENT '购买数量',
  `price` DECIMAL(10,2) NOT NULL COMMENT '单价',
  `total_amount` DECIMAL(10,2) NOT NULL COMMENT '总金额',
  `seller_id` INT COMMENT '卖家ID',
  `status` TINYINT DEFAULT 0 COMMENT '订单状态：0-待支付，1-已支付，2-已发货，3-已完成，4-已取消',
  `created_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  INDEX idx_user (`user_name`),
  INDEX idx_order_no (`order_no`),
  INDEX idx_status (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='订单表';
```

##### 12.2.5 product_images（商品图片表）
```sql
CREATE TABLE `product_images` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '图片ID',
  `product_id` BIGINT NOT NULL COMMENT '商品ID',
  `image_url` VARCHAR(500) NOT NULL COMMENT '图片URL',
  `sort_order` INT DEFAULT 0 COMMENT '排序',
  `created_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  INDEX idx_product (`product_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='商品图片表';
```

---

继续下一部分...
