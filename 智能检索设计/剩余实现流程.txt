# 智能检索系统 - 剩余实现流程
> 创建日期：2026-03-21
> 最后更新：2026-03-29
> 对齐依据：截至 2026-03-29 的实际代码状态

本文档只列“剩余工作”，不再重复已经完成的 W00-W03 全量内容。

---

## 一、当前已经完成到什么程度

已完成或基本完成：
- W00 统一口径
- W01 工程骨架
- W02 快照层
- W03 `product_search` ES 主链
- W04 Runtime 基础骨架
- W05 Python LangChain Query Parser
- W06 参数标准化主链
- W07 Python LangGraph Tool Router
- W08 Composer 主链骨架
- W09 知识层主链
- W10 实时层主链
- W11 多轮会话主链与冻结回归

当前剩余工作不再是“从零搭建”，而是以下 5 类：

1. W12 同步补偿重建收尾
2. W13 监控评估回放
3. W08/W09/W10 收尾
4. 生产级联调与上线准备
5. 文档与治理口径持续收口

---

## 二、当前最重要的剩余工作

### 1. W12 同步、补偿、重建收尾

这是当前正在进行的阶段，也是生产前必须完成的阶段。

#### 当前已完成
- [x] `SyncAdminService` / `SyncAdminController` 统一 sync admin 入口
- [x] 产品链路 `status / reconcile / rebuild / repair`
- [x] 知识链路 `status / reconcile / rebuild / repair`
- [x] 产品、知识正式增量入口
- [x] `SyncTaskRecord / SyncTaskTracker` 任务记录
- [x] 失败重试与死信查询
- [x] 产品、知识按 `entity_id` 列表、`updated_at` 范围、状态批量补偿
- [x] 产品按 `categoryId` 批量补偿
- [x] 知识按 `entityType/entityId` 批量补偿
- [x] 产品、知识 detail reconcile 明细接口
- [x] 产品、知识按 detail reconcile 结果批量修复
- [x] 产品、知识全局 mismatch detail 接口
- [x] 产品、知识按全局 reconcile 结果批量修复
- [x] `report/baseline` 对账基线产物
- [x] `products/index/validate` alias 校验入口
- [x] `products/index/rollback/candidates` 与 `products/index/rollback/auto`
- [x] 版本索引重建、alias 切换、回滚、定向索引补偿入口

#### 剩余项
- [ ] alias 重建与回滚自动化校验最后收口
- [ ] 更完整的修复策略治理
- [ ] 基于 baseline report 接入 W13 replay / golden cases

### 2. W13 监控、评估、治理

这一阶段已经启动首批后端能力，但还远未收口。

剩余项：
- [x] replay 数据结构与回放入口
- [x] eval run 历史落库与查询入口
- [x] release status / overview / metrics summary / metrics trend
- [x] release records / gray configs / release preflight
- [x] replay -> golden case bootstrap
- [x] eval run compare
- [x] golden case 批量导入 / 导出 / 批量启停
- [x] metrics daily 日聚合持久化
- [x] 错误归因 summary
- [x] golden case 版本快照
- [x] 基于版本快照直接运行 eval regression
- [x] eval run 按 `sourceType/versionId` 过滤
- [x] release preflight 支持固定版本回归检查
- [x] release record 绑定版本与基线校验
- [x] release record 直接触发绑定版本回归
- [x] 批量版本回归入口
- [x] 版本详情运行概览：`latestRun / recentRuns`
- [x] replay checkpoint / tool I/O 细粒度落库与 detail 查询
- [x] 版本快照 compare：新增/删除/规则变化对比
- [x] gray config 详情 / 应用到 release record / release 状态流转
- [x] `metrics/error-attribution/trend`
- [x] `eval-cases/stats`
- [x] replay 候选池：按时间范围、问题样本、是否已存在 query 过滤
- [x] replay 批量 bootstrap：从 replay 批量生成 eval case / golden case
- [x] release 事件历史：创建、gray config 应用、状态流转、回归执行留痕
- [ ] golden cases / regression set 深化
- [ ] 日常指标与错误归因维度
- [ ] 发布、灰度、版本控制流程深化

### 3. W08/W09/W10 收尾

虽然这三块主链都已经可运行，但仍有尾项没收完。

W08：
- [ ] citation 规则细化
- [ ] entity merge / conflict resolution 增强
- [ ] 更稳定的表达层治理

W09：
- [ ] chunk 模型
- [ ] 知识同步链路
- [ ] 上下线治理

W10：
- [ ] 真实 realtime gateway 联调
- [ ] 限流、监控、熔断验证

---

## 三、按阶段拆分的剩余实现流程

### 阶段 5：W12 同步、补偿、重建

这是当前正在进行的阶段。

建议顺序：
1. 补 alias 自动校验与回滚验证
2. 补修复策略治理与固化
3. 基于 baseline report 衔接 W13 回放与评估输入
4. 转入 W13

### 阶段 6：W13 监控、评估、回放

依赖 W12 收口。

建议顺序：
1. golden cases / regression set 深化
2. 灰度与版本治理深化
3. 指标趋势持久化与更多归因
4. 衔接 W14 联调上线

### 阶段 7：W14 联调、压测、上线

依赖 W12/W13。

建议顺序：
1. 联调
2. 压测
3. 故障演练
4. 上线收尾

---

## 四、当前推荐推进顺序

1. 以 W11 冻结回归为基线
2. 继续收尾 W12
3. 并行收尾 W08/W09/W10
4. 继续推进 W13
5. 最后做 W14

从 2026-03-29 开始，最近一步应该是：

**继续收尾 W12，并推进 W13 的发布治理、更多归因趋势与回归池治理。**
